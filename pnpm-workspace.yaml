# Build-script approval for the @earendil-works/pi-coding-agent peer graph
# (pulled via @cad0p/pi-steering): genai's postinstall is needed for its JS
# runtime; protobufjs works without its postinstall. (blockExoticSubdeps no
# longer needed — all deps are registry ranges since the 0.1.0 publish.)
allowBuilds:
  '@google/genai': true
  protobufjs: false
